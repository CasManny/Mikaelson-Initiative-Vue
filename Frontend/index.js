import { createRouter, createWebHistory } from 'vue-router'

// Only keep essential routes that don't conflict with main router
const routes = [
  // This file should not have duplicate routes
  // All routes are handled by src/router/index.js
]

// Router is handled by src/router/index.js
// This file is kept for potential future use but should not export a router

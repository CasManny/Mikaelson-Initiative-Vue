import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mikaelson.initiative',
  appName: 'Mikaelson Initiative',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
}

export default config
